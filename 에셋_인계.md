# 무명극 / CURTAIN CALL — 에셋 인계

생성 스크립트: `C:\Users\kimsijun\Desktop\Meshy3DAPI`
스타일 근거: `NCAI MCP\docs\무명극_Meshy_스타일가이드.md`
구역별 오브젝트 목록: 같은 폴더의 `투사_…`, `퇴행_…`, `커튼_문_…`, `무대_단상_…` 문서

---

## 1. 뭐가 어디에 있나

### 3D 모델 — `Assets\05. Models\`

| 폴더 | 내용 |
|------|------|
| `Stage\` | 무대(승화) 구역. 번호 `01~19` = 배경 소품, 이름 = 게임플레이 오브젝트 |
| `Projection\` | 투사(집) 구역. `proj_*` |
| `Regression\` | 퇴행(발레실) 구역. `regr_*` 24종 |

**Stage 안의 이름 규칙**

- `01_curtain_grand` ~ `19_hand_bell` — 배경 소품 (20 장미는 생성 실패로 없음)
- `platform_riser` · `platform_crate` · `platform_plank_hanging` — 점프 발판
- `pipe_broken_end` · `pipe_cracked` — 가스 위험물
- `switch_lever` — 상호작용 스위치

**Projection 안**

- `proj_vanity` (화장대) · `proj_broken_mirror` (깨진 거울, 구역 심볼)
- `proj_tilted_door` (기울어진 방문) · `proj_workbook` (문제집 더미)
- `proj_pencil.obj` + `.mtl` — 절차적 생성. 삼각형 72개, 실제 치수 17.5 cm

### 텍스처 — `Assets\Textures\`

| 폴더 | 내용 |
|------|------|
| `Stage\` | `stage_wall_paper` (벽지) · `stage_floor_audience` (관객석 카펫) · `stage_floor_aisle` (통로 마루) — 전부 albedo + normal 쌍 |
| `Projection\` | `proj_recital_poster` · `proj_exam_form` — Quad에 붙이는 2D 소품 |
| `Regression\` | `regr_floor_sprung` — 발레실 마루 |
| `VFX\` | `smoke_puff_sheet` — 연기 4프레임 스프라이트 시트 |

타일링 텍스처는 상하좌우로 이음매 없이 반복된다. 1024×1024.

### UI·브랜딩 — `Assets\04. Images\`

| 경로 | 내용 |
|------|------|
| `Cursor\` | 기본/상호작용 커서, 32·48·64·128px |
| `UI\LogoScene\` | `CurtainCallLogo` (워드마크) · `_Full` (커튼 프레임) · `Icon` (정사각) · `Wordmark_Dark` (밝은 배경용) |

---

## 2. Unity 설정 — 놓치면 안 되는 것

### ① 머티리얼 Albedo를 반드시 교체

**FBX에 내장된 텍스처는 색 보정 전 원본이다.** 함께 들어온 `*_albedo.png`를 지정할 것.
안 하면 채도·틴트 보정이 통째로 무효가 된다.

| 대상 | 적용된 보정 |
|------|------------|
| 무대·플랫폼·토관·퇴행 | 채도 0.55 |
| `switch_lever` | 채도 0.85 (상호작용물이라 덜 눌렀다) |
| `proj_vanity` | 채도 0.25 + 명도 0.88 + 로즈그레이 틴트 |
| `12_podium`, `proj_workbook` | 채도·명도 + 틴트 (색 이탈 교정) |

공통: **Metallic 0, Smoothness 낮게** (가이드 §5-5).

### ② `_normal` 텍스처는 Texture Type을 `Normal map`으로

Unity가 파일명으로 자동 인식하지 않는다. 그냥 두면 파랗게 보인다.

### ③ 타일링 텍스처는 Wrap Mode: Repeat

Clamp면 반복이 안 된다. 바닥은 타일 1장 = 1~2 m 가 자연스럽다.

### ④ 2D·스프라이트는 Alpha Is Transparency 켜기

포스터·응시원서·연기·커서 전부 해당.

---

## 3. 오브젝트별 주의사항

- **점프 플랫폼 3종** — Box Collider를 직접 잡을 것. 메시 표면이 고르지 않아
  Mesh Collider로는 착지가 튄다. 크기도 제각각이라 점프 높이 기준으로 스케일 통일 필요.
  `platform_plank_hanging`은 지지 프레임이 같이 붙어 있다.
- **가스 토관** — 메시에 가스는 없다. 구멍에 파티클 emitter, 데미지는 가스 영역에 Trigger.
- **`switch_lever`** — Emissive는 상아색 손잡이에만. 판 전체에 주면 매트 느낌이 죽는다.
- **연기 시트** — Texture Sheet Animation `Tiles X=4, Y=1`. 뼈색으로 구웠으니
  Start Color로 틴트해 돌려 쓸 것 (가스 `#8A9464` / 먼지 `#9A9288` / 무대 연막 `#9C7A80`).
- **커서** — 핫스팟은 화살표 팁. 64px 텍스처 기준 `(4, 4)`, 32px면 `(2, 2)`.
- **`proj_pencil.obj`** — `.mtl`의 색이 머티리얼 5개로 자동 생성된다. 스케일 1로 둘 것.

---

## 4. 알려진 문제

| 항목 | 내용 |
|------|------|
| **용량** | `05. Models`가 1.1 GB. FBX마다 2K 텍스처 내장 + albedo PNG 별도. albedo를 512로 줄이면 1/5 수준으로 떨어진다 (크레딧 0) |
| **퇴행 24종 톤** | 전반적으로 희고, 일부에 의도치 않은 분홍 리본이 붙어 있다. 흰색은 틴트 후처리로 잡히지만(크레딧 0) 리본은 재생성해야 없어진다 |
| **20 장미 다발** | 세 번 시도 모두 실패. 꽃·가는 줄기는 Meshy가 못 만든다 |
| **`regr_fallen_slipper`** | 원래 '부서진 토슈즈'였으나 파손 상태가 세 번 다 안 나와 온전한 소품으로 용도 변경. 장애물 판정은 Trigger로 |
| **`stage_floor_audience`** | 메달리온 문양이 격자에 딱 맞아 물방울무늬처럼 읽힌다 |
| **밸브 토관** | 관에 글자가 각인되어 제외 (§8 위반) |

---

## 5. 다시 만들 때

```bash
# 3D 오브젝트 (--zone: stage / projection / regression / rationalization / stage_accent)
python generate.py "영문 형태 묘사" --zone regression

# 색 보정만 다시 (크레딧 0)
python desaturate.py output --factor 0.55
python desaturate.py output --factor 0.25 --brightness 0.88 --tint "206,184,186"

# 2D 에셋 (크레딧 0)
python textures.py --dest "...\Assets\Textures\Stage"
python paper_props.py --dest "...\Assets\Textures\Projection"
python smoke.py ; python pencil.py ; python cursor.py ; python logo.py

# Unity로 복사 (구역별 폴더로 자동 분배)
python export_to_unity.py
```

### 이번 작업에서 확인된 두 가지

**색은 프롬프트로 잡지 말고 후처리로 잡는다.** 채도·색조·명도를 프롬프트로 통제하려는
시도는 다섯 번 전부 실패했다. 오브젝트마다 다르게 해석되고, 네거티브를 세게 걸면
팔레트 반대편 색조로 도망간다(올리브 그린·마젠타·분홍). `desaturate.py`의 계수 방식은
한 번에 해결됐고 세트 전체가 일관된다.

**Meshy는 덩어리진 물건에 강하고, 얇거나 가늘거나 기하학적으로 단순한 물건에 약하다.**
포스터·응시원서(종이), 연필, 장미, 부서진 조각이 모두 실패했다. 밖에서 만든 결과가
형태·용량 양쪽에서 나았다 (연필: 8 MB → 1.9 KB). 비슷한 형태는 먼저 이 판단을 할 것.

**팔레트 문구에 형태를 뜻하는 명사를 넣지 말 것.** 퇴행 팔레트에
`faded dusty pink accent only on ribbons`라고 썼더니 색 한정이 아니라 형태 지시로 읽혀
라디에이터·벽시계·사물함까지 전부 나비 리본이 달려 나왔다.
