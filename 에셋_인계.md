# 무명극 에셋 인계 — Meshy 생성분

생성 스크립트 위치: `C:\Users\kimsijun\Desktop\Meshy3DAPI`
스타일 근거: `NCAI MCP\docs\무명극_Meshy_스타일가이드.md`

---

## 1. 들어온 에셋

### `Assets\05. Models\Stage\` — FBX 22개 (+ albedo 22장)

| 그룹 | 파일 |
|------|------|
| 무대 배경 소품 (16) | `02_backdrop`, `03_proscenium_arch`, `04_chandelier`, `05_balcony_box`, `06_stage_platform`, `08_footlight_lamp`, `09_theater_seat`, `10_music_stand`, `11_prop_trunk`, `13_wall_frame`, `14_wall_sconce`, `15_mask`, `16_wall_clock`, `17_curtain_rod`, `18_candelabra`, `19_hand_bell` |
| 점프 플랫폼 (3) | `platform_riser`, `platform_crate`, `platform_plank_hanging` |
| 가스 위험물 (2) | `pipe_broken_end`, `pipe_cracked` |
| 상호작용 (1) | `switch_lever` |

번호가 붙은 것은 배경 소품, 이름만 있는 것은 게임플레이 오브젝트다.

### `Assets\05. Models\Projection\`

| 파일 | 비고 |
|------|------|
| `proj_vanity.fbx` + `_albedo.png` | 거울이 비어 있는 v2 |
| `proj_pencil.obj` + `.mtl` | 절차적 생성. 삼각형 72개, 실제 치수 17.5 cm |

### `Assets\Textures\Stage\` — 타일링 텍스처 (albedo + normal 쌍)

`stage_wall_paper` (극장 벽지) · `stage_floor_audience` (관객석 카펫) · `stage_floor_aisle` (통로 마루)

상하좌우 이음매 없이 반복된다. 1024×1024.

### `Assets\Textures\Projection\`

`proj_recital_poster_albedo.png` · `proj_exam_form_albedo.png`

Quad에 붙이는 2D 소품. 알파 포함 1024×1024.

### `Assets\Textures\VFX\`

`smoke_puff_sheet.png` — 연기 퍼프 4프레임, 1024×256, 알파 포함.

---

## 2. 임포트 설정

### 모든 FBX 공통 — Albedo를 바꿔야 한다

**FBX에 내장된 텍스처는 색 보정 전 원본이다.** 머티리얼 Albedo에 반드시 함께 들어온
`*_albedo.png`를 지정할 것. 안 하면 채도 보정이 통째로 무효가 된다.

| 대상 | 적용된 보정 |
|------|------------|
| 무대 소품·플랫폼·토관 | 채도 0.55 |
| `switch_lever` | 채도 0.85 (상호작용물이라 덜 눌렀다) |
| `proj_vanity` | 채도 0.25 + 명도 0.88 + 로즈그레이 틴트 |

공통: **Metallic 0, Smoothness 낮게** (스타일가이드 §5-5). 클레이는 광택이 없어야 한다.

### `_normal` 텍스처 3장

Texture Type을 **`Normal map`** 으로 바꿀 것. Unity가 파일명으로 자동 인식하지 않아서,
그냥 두면 노멀맵이 색 텍스처로 취급돼 파랗게 보인다.

### 타일링 텍스처

**Wrap Mode: Repeat** (Clamp면 반복이 안 된다). 바닥은 타일 1장을 1~2 m로 잡으면 자연스럽다.

### 2D 종이 소품 / 연기 스프라이트

**Alpha Is Transparency 켜기.** 머티리얼은 Unlit 또는 Lit(매트).

---

## 3. 오브젝트별 주의사항

### 점프 플랫폼 3종

- **Box Collider를 직접 잡을 것.** Meshy 메시는 표면이 미세하게 울퉁불퉁해서
  Mesh Collider를 걸면 착지가 튄다. 메시는 보여주기용으로만 쓴다.
- **셋 다 크기가 제각각이다.** 점프 높이 기준으로 스케일을 통일해야 한다.
- `platform_plank_hanging`은 지지 프레임(가로대·기둥)이 같이 붙어 있다.
  천장에서 내려오는 공중 발판으로 쓰려면 프레임을 지우고 밧줄만 위로 늘릴 것.

### 가스 토관 2종

메시에 가스는 없다. 깨진 단면·갈라진 틈이 배출구다.

- 파티클 emitter를 구멍에 배치. `smoke_puff_sheet`를 쓰면 된다.
- 데미지 판정은 관이 아니라 **가스가 퍼지는 영역에 Trigger**로 잡는 편이 자연스럽다.

### `switch_lever`

- 레버 각도로 on/off를 표현할 수 있다 (상태별 프리팹 또는 레버만 회전).
- **Emissive는 상아색 손잡이에만.** 판 전체에 주면 클레이 매트 느낌이 죽는다.
  스타일가이드 §3-3: "메시는 매트, 빛은 Unity에서".

### `proj_pencil.obj`

`.mtl`의 색이 머티리얼 5개로 자동 생성된다 (몸통·나무·흑연·금속밴드·지우개).
`Ks 0`이라 하이라이트가 없으니 그대로 매트다. 실제 치수라 스케일 1로 둘 것.

거대 스케일 연출이 필요하면 스케일을 올리지 말고 다시 생성하는 편이 낫다:
`python pencil.py --length 0.60`

### 연기 스프라이트

Texture Sheet Animation 모듈에서 **Tiles X = 4, Y = 1**.

뼈색(`#E8E2D6`)으로 구웠으니 파티클 Start Color로 틴트해서 돌려 쓸 것:

| 용도 | Start Color |
|------|-------------|
| 토관 가스 | `#8A9464` (탁한 황록) |
| 먼지·착지 | `#9A9288` |
| 무대 연막 | `#9C7A80` |

---

## 4. 아직 없는 것 / 알려진 문제

### 생성 실패해서 안 들어온 것

| 오브젝트 | 문제 |
|---------|------|
| 01 대형 극장 커튼 | 올리브 그린 + 마젠타로 팔레트 이탈 |
| 07 무대 계단 | 계단으로 안 읽힘 (납작한 판) |
| 12 연단 | 연단 위에 생물체 — §8 'no character' 위반 |
| 20 장미 다발 | 거의 검정, 형태가 안 읽힘 |
| 밸브 달린 토관 | 관에 글자가 각인됨 — §8 위반 |

재생성 후 `python export_to_unity.py` 한 번이면 갱신된다.

### 품질 이슈

- **`05. Models\Stage` 폴더가 603 MB.** FBX마다 2K 텍스처가 내장된 데다 albedo PNG가
  따로 붙어서 그렇다. 2,000 트라이앵글 프롭치고 과하다.
  albedo를 512로 줄이면 50 MB 수준까지 떨어진다.
- **`stage_floor_audience` 문양이 규칙적이다.** 메달리온이 격자에 딱 맞아서
  물방울무늬처럼 읽힌다.
- **`stage_floor_aisle` 노멀 강도(14)가 세다.** 이음매 홈이 조명 각도에 따라
  과하게 파여 보일 수 있다.
- **`Meshy_AI_claymation_stage_plat_0809121433_image-to-3d-texture.fbx`** 는
  스크립트 관리 대상이 아니다 (직접 받으신 것으로 보임).

---

## 5. 다시 만들 때

```bash
# 3D 오브젝트 하나
python generate.py "영문 설명" --zone stage      # projection / rationalization / regression / stage_accent

# 무대 20종 배치
python batch_stage.py --only 1 7 12 20

# 색 보정만 다시 (크레딧 0)
python desaturate.py output --factor 0.55
python desaturate.py output --factor 0.25 --brightness 0.88 --tint "206,184,186"

# 2D 에셋 (크레딧 0)
python textures.py --dest "...\Assets\Textures\Stage"
python paper_props.py --dest "...\Assets\Textures\Projection"
python smoke.py
python pencil.py
python cursor.py

# Unity로 복사
python export_to_unity.py
```

### 알아둘 것

**색은 프롬프트로 잡지 말고 후처리로 잡는다.** 프롬프트로 채도·색조·명도를 통제하려는
시도는 다섯 번 전부 실패했다 (오브젝트마다 다르게 해석되고, 네거티브를 세게 걸면
팔레트 반대편 색조로 도망간다). `desaturate.py`의 계수 방식은 한 번에 해결됐고
세트 전체가 일관된다.

**Meshy는 덩어리진 물건에 강하고 얇거나 가늘거나 기하학적으로 단순한 물건에 약하다.**
포스터·응시원서(얇은 종이)와 연필(가늘고 긴 물체)은 Meshy에서 각각 은박지 뭉치와
첨탑 구조물로 나왔다. 밖에서 만든 결과가 형태·용량 양쪽에서 나았다
(연필: 8 MB → 1.9 KB). 비슷한 형태의 요청은 먼저 이 판단을 할 것.
